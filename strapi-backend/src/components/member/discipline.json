{
  "collectionName": "components_member_disciplines",
  "info": {
    "displayName": "Discipline Ranking",
    "description": "Hồ sơ xếp hạng của hội viên theo một bộ môn — mỗi bộ môn có điểm/hạng/số trận riêng"
  },
  "options": {},
  "attributes": {
    "category": {
      "type": "enumeration",
      "enum": ["Pool 8 bi", "Pool 9 bi", "Pool 10 bi", "Carom 1 băng", "Carom 3 băng", "Snooker", "English Billiards"],
      "required": true
    },
    "points": { "type": "integer", "default": 0 },
    "rank": { "type": "integer" },
    "matches": { "type": "integer", "default": 0 },
    "trend": {
      "type": "enumeration",
      "enum": ["up", "down", "eq"],
      "default": "eq"
    },
    "trendValue": { "type": "integer", "default": 0 }
  }
}
