'use strict';
/* Xem src/utils/public-forms.js — chỉ nhận đúng field cho phép, tự đặt trạng thái. */
const forms = require('../../../utils/public-forms');
module.exports = { submit: forms.tournamentRegistration };
